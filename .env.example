# Default client relay base.
# Valid values: local, /api/live, same-origin, or a full https://relay.example.com/api/live URL.
VITE_LIVE_RELAY_URL=/api/live

# Dev-only target for vite.config.js when /api/live is proxied locally.
GTE_DEV_LIVE_RELAY_TARGET=http://127.0.0.1:4180